import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  table: {
    minWidth: 650,
  },
  tableHeader: {
    fontWeight: 'bold',
    fontSize: '16px',
  },
})

export default useStyles
